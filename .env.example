# Food Classification Configuration
CONFIDENCE_THRESHOLD=0.6

# Model paths (update these to point to your actual trained models)
# YOLO_WEIGHTS=path/to/your/yolo_best.pt
# CLF_CKPT=path/to/your/efficientnet_best.pth

# Google Cloud Configuration (if using cloud storage)
# GCS_BUCKET_NAME=your-bucket-name
# MODEL_BUCKET_NAME=your-model-bucket
# MODEL_FILENAME=latest_model.keras
# GOOGLE_CLOUD_PROJECT=your-project-id

# API Keys
# GEMINI_API_KEY=your-gemini-api-key